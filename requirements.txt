# offensive-scripts requirements
# Install: pip install -r requirements.txt

# --- Used by ---
paramiko>=2.12.0        # ssh_scanner.py   — SSH auth, transport, key handling
dnspython>=2.6.1        # dns_scanner.py   — DNS resolver
ipwhois>=1.3.0          # ipmagic.py       — WHOIS / ASN lookups
requests>=2.31.0        # dns_scanner.py   — HTTP requests
